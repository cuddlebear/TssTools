<h1>Listing area</h1>

<table>
  <tr>
    <th>Domain</th>
    <th>Name</th>
    <th>Filter</th>
    <th>Filtertype</th>
    <th>Sortorder</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @areas.each do |area| %>
  <tr>
    <td><%= area.domain %></td>
    <td><%= area.name %></td>
    <td><%= area.filter %></td>
    <td><%= area.filterType %></td>
    <td><%= area.sortOrder %></td>
    <td><%= link_to 'Show', area %></td>
    <td><%= link_to 'Edit', edit_area_path(area) %></td>
    <td><%= link_to 'Destroy', area, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Domain area', new_area_path %>
