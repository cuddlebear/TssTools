<%= form_for(@page, html: {class: "form-horizontal"}) do |f| %>
    <%= f.hidden_field :domain_id %>
    <% if @page.errors.any? %>
        <div id="errorExplanation"  class="alert alert-error">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <h3><%= pluralize(@page.errors.count, "error") %> prohibited
            this Url from being saved:</h3>
          <ul>
            <% @page.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="control-group">
      <%=  f.label :active, "Active", class: "control-label" %>
      <div class="controls"><%= f.check_box :active %></div>
    </div>

    <div class="control-group<% if @page.errors["path"].any? %> error<% end %>">
      <%=  f.label :path,"Url", class: "control-label" %>
      <div class="controls"><%= f.text_field :path, class: "input-xxlarge" , placeholder: "Page" %></div>
    </div>

    <div class="control-group<% if @page.errors["name"].any? %> error<% end %>">
      <%=  f.label :name , "Title", class: "control-label" %>
      <div class="controls"><%= f.text_field :name, class: "input-xxlarge" , placeholder: "Title" %></div>
    </div>

    <%= f.submit class: "btn btn-primary btn-small" %>
<% end %>