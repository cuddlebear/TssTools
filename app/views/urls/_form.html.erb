<%= form_for(@url, html: {class: "form-horizontal"}) do |f| %>
    <%= f.hidden_field :domain_id %>
    <% if @url.errors.any? %>
        <div id="errorExplanation"  class="alert alert-error">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <h3><%= pluralize(@url.errors.count, "error") %> prohibited
            this Url from being saved:</h3>
          <ul>
            <% @url.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="control-group<% if @url.errors["path"].any? %> error<% end %>">
      <label class="control-label" for="inputPath">Url</label>
      <div class="controls"><%= f.text_field :path, class: "input-xxlarge" , placeholder: "Url" %></div>
    </div>

    <%= f.submit class: "btn btn-primary btn-small" %>
<% end %>